package com.on.eye.api.demoInfo.repository;

import com.on.eye.api.demoInfo.entity.DemoPostImage;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DemoPostImageRepository extends JpaRepository<DemoPostImage, Long> {

}